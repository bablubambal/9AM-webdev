1. Create a virtural env
 python -m venv .venv
 2. which virtural env to be used
  windows: ctl +shift + p

3. ctrl+j ( to run new virtual env)
4. pip install flask
5.create a file app.py write the bare minimal app

from flask import Flask 
app = Flask(__name__)

command to run flask app
flask run

stores = [
  
]